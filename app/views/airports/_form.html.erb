<%= form_for(@airport) do |f| %>
	<% if @airport.errors.any? %>
		<div id="error_explanation">
		  <h2><%= pluralize(@airport.errors.count, "error") %> prohibited this airport from being saved:</h2>

		  <ul>
			<% @airport.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		  </ul>
		</div>
	<% end %>

	<table>
	  <tr>
		<td><%= f.label :code %></td>
		<td><%= f.text_field :code %></td>
	  </tr>
	  <tr>
		<td><%= f.label :name %></td>
		<td><%= f.text_field :name %></td>
	  </tr>
	  <tr>
		<td><%= f.label :city %></td>
		<td><%= f.text_field :city %></td>
	  </tr>
	  <tr>
		<td><%= f.label :state %></td>
		<td><%= f.text_field :state %></td>
	  </tr>
	  <tr>
		<td><%= f.label :latitude %></td>
		<td><%= f.text_field :latitude %></td>
	  </tr>
	  <tr>
		<td><%= f.label :longitude %></td>
		<td><%= f.text_field :longitude %></td>
	  </tr>
	  <tr>
		<td><%= f.label :wac %></td>
		<td><%= f.number_field :wac %></td>
	  </tr>
	  <tr>
		<td><%= f.label :notes %></td>
		<td><%= f.text_field :notes %></td>
	  </tr>
	  <tr>
		<td><%= f.label :country_id %></td>
		<td><%= f.text_field :country_id %></td>
	  </tr>
	</table>
	<div class="actions">
	  <%= f.submit %>
	</div>
<% end %>
